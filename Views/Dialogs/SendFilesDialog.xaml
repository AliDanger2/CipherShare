<Window x:Class="CipherShare.Views.Dialogs.SendFilesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Send files" Height="540" Width="480"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResizeWithGrip"
        Background="{StaticResource BackgroundBrush}"
        AllowDrop="True" Drop="Window_Drop">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock Text="Send files" FontSize="17" FontWeight="Bold"/>
            <TextBlock Text="{Binding TargetLabel, StringFormat='to {0}'}" FontSize="12"
                       Foreground="{StaticResource TextMutedBrush}" Margin="0,2,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8"
                Padding="16" Margin="0,0,0,14">
            <StackPanel HorizontalAlignment="Center">
                <TextBlock Text="Drag and drop files or folders here" FontSize="12" HorizontalAlignment="Center"
                           Foreground="{StaticResource TextSecondaryBrush}"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                    <Button Content="Add Files..." Style="{StaticResource SecondaryButtonStyle}"
                            Command="{Binding AddFilesCommand}"/>
                    <Button Content="Add Folder..." Style="{StaticResource SecondaryButtonStyle}" Margin="8,0,0,0"
                            Command="{Binding AddFolderCommand}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="No files selected yet." FontSize="12" Foreground="{StaticResource TextMutedBrush}"
                           Visibility="{Binding HasItems, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
                <ItemsControl ItemsSource="{Binding Items}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="6" Padding="10,8" Margin="0,0,0,6">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding Name}" FontSize="12" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock FontSize="10" Foreground="{StaticResource TextMutedBrush}">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="File"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsFolder}" Value="True">
                                                            <Setter Property="Text" Value="Folder (sent recursively)"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                    <Button Grid.Column="1"
                                                Style="{StaticResource IconButtonStyle}"
                                                Width="24"
                                                Height="24"
                                                Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}">
                                        <Viewbox Width="12"
                                                     Height="12">
                                            <Path Style="{StaticResource LucideIconStyle}"
                                                      Data="{StaticResource CloseIcon}"/>
                                        </Viewbox>
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Style="{StaticResource GhostButtonStyle}" Padding="14,8"
                    Command="{Binding CancelCommand}"/>
            <Button Content="Send" Style="{StaticResource PrimaryButtonStyle}" Padding="18,8" Margin="8,0,0,0"
                    Command="{Binding SendCommand}"/>
        </StackPanel>
    </Grid>
</Window>
